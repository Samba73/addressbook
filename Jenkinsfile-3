pipeline {
  agent { dockerfile true }
  stages {
    stage('tomcat server') {
      steps {
        sh '''
          node --version
          git --version
          curl --version
        '''
      }
    }
  }
}
